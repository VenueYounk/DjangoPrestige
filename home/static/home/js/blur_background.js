// const bluring_background = document.querySelector('.blur_background');

// window.addEventListener('scroll', () => {
//     const scrollValue = window.scrollY;
//     const maxBlur = 30; // Максимальная степень замыления (измените на свое усмотрение)
//     const blurValue = Math.min(scrollValue / 50, maxBlur); // Измените значение 10 на свое усмотрение
//     bluring_background.style.filter = `blur(${blurValue}px)`;
    
//     if (scrollValue >= 1000) {
//         bluring_background.style.filter = `blur(${maxBlur}px)`; // Ограничиваем замыление на 500 пикселей скролла
//     }
// });